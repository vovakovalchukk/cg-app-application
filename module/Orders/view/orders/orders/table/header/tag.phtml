<?php $this->inlineScript()->captureStart() ?>
require(["Orders/tag"], function(TagBulkAction)
{
    var tagBulkAction = new TagBulkAction(n);
    $(document).on("change", "#<?= $id ?> input.tag[data-tag='<?= $tag ?>']", function()
    {
        tagBulkAction.checkbox($(this));
    });
});
<?php $this->inlineScript()->captureEnd() ?>
LKAJDS
<?= $tag ?>