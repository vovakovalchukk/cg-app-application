<?php $this->inlineScript()->captureStart(); ?>
function Foo()
{
    this.$body = $('html, body');
    this.headerHeight = $('#fixed-header').height() + $('#sub-header').height();

    var that = this

    this.a = function() {
        $('a').click(function() {
            console.log("bar()");
            var href = $.attr(this, 'href');
            that.$body.animate({
                scrollTop: $( href ).offset().top - that.headerHeight
            }, 200,  function () {
                window.location.hash = href;
            });
            return false;
        });
    }
}

var foo1 = new Foo();
foo1.a();

<?php $this->inlineScript()->captureEnd(); ?>
<div class="module filter-module clearfix">
    <h3>Sections</h3>
    <div class="module-content nav">
        <ul>
            <li><a href="#order-status">Order Status</a></li>
            <?php foreach($this->links as $link): ?>
                <li><a href="#<?= $link ?>"><?= $this->translate($link); ?></a></li>
            <?php endforeach;?>
        </ul>
    </div>
</div>