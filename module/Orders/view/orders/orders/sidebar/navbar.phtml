<?php $this->inlineScript()->captureStart(); ?>
var $root = $('html, body');
$('a').click(function() {
  $root.animate({
    scrollTop: $( $.attr(this, 'href') ).offset().top - 119
  }, 200); // TODO add hash to href
  return false;
});
<?php $this->inlineScript()->captureEnd(); ?>
<h2>Sections</h2>
<ul>
<?php foreach($this->links as $link): ?>
    <li><a href="#<?= $link ?>"><?= ucwords(implode(" ", preg_split("/(?=[A-Z])/", $link))); ?></a></li> <!-- TODO use translator here -->
<?php endforeach;?>
</ul>