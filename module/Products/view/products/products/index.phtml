<?php $this->inlineScript()->captureStart(); ?>
require.config({
    paths: {
        'Stock': '<?= $this->basePath() . Products\Module::PUBLIC_FOLDER; ?>/js/Stock',
        'Product': '<?= $this->basePath() . Products\Module::PUBLIC_FOLDER; ?>/js/Product',
        'Variation': '<?= $this->basePath() . Products\Module::PUBLIC_FOLDER; ?>/js/Variation'
    }
});
require([
    "showHideFilters", "Product/DomListener/Search", "jquery"
],
function(ShowHideFilters, searchDomListener, $) {
    $(document).ready(function()
    {
        var hideFilters = new ShowHideFilters();
        searchDomListener.init('.product-search-text', '.product-search-button', '<?= $this->basePath() . Products\Module::PUBLIC_FOLDER; ?>img');
    });
});
<?php $this->inlineScript()->captureEnd(); ?>
<?php $this->placeholder('#main.class')->append('full-width'); ?>
<?php
$this->placeholder('#sidebar')
    ->append($this->sidebarLinks);
?>
<?php $this->headLink()->appendStylesheet($this->basePath() . Orders\Module::PUBLIC_FOLDER . 'css/orders.css') ?>
<div id="main" class="transition-preload module clearfix<?= $this->placeholder('#main.class')->setPrefix(' ')->setIndent(1) ?>">
<?= $this->breadcrumbs; ?>
<?= $this->bulkItems; ?>
    <div id="products-list">
    </div>
    <div id='products-loading-message' class='dataTables_processing'>
        Loading products...
    </div>
</div>
<script type="text/javascript">
    require(['Variation/Service'], function(variationService)
    {
        variationService.init();
    });
</script>
<style>
/**
  This is a hack to hide the sidebar away for now TODO: Remove this
  When introducing filters (ie Saved Filters)
**/
#main-wrapper {
    margin-left:0px;
    width:100%;
}
</style>
