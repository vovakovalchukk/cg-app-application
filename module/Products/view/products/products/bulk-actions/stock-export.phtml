require(["Product/BulkAction/StockExport"], function(StockExport)
{
    var stockExportAction = new StockExport(
        "<?= $this->translate('Preparing to generate stock export') ?>",
        "<?= $this->translate('Generating stock export') ?>",
        "<?= $this->translate('Finished generating stock export') ?>"
    );
    stockExportAction.init('<?=$selector;?>');
});