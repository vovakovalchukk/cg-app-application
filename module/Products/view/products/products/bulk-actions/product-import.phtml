require.config({
    paths: {
        "Product/BulkAction/ProductImport": "<?= $this->baseUrl . Products\Module::PUBLIC_FOLDER . 'js/Product/BulkAction/ProductImport' ?>"
    }
});

require(["Product/BulkAction/ProductImport"], function(ProductImport)
{
    var productImportAction = new ProductImport("<?= $selector ?>");
    productImportAction.init(<?= json_encode($this->templateMap) ?>);
});
