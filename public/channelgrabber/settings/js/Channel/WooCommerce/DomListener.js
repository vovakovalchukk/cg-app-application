define([], function() {
    var DomListener = function() {};

    DomListener.DOM_SELECTOR_ACCOUNT_FORM = '#woocommerce-form';

    DomListener.prototype.init = function(saveUrl, accountId) {

    };

    return new DomListener();
});
