define(['jQuery'], function($)
{
    var DomManipulator = function()
    {

    };

    DomManipulator.prototype.insertTemplateHtml = function(html)
    {
        /*
         * TODO (CGIV-2009)
         * Use jQuery to instert the HTML in the right place
         */
    };

    return new DomManipulator();
});